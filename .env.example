# GitHub fine-grained personal access token
# Required scopes: contents (read/write), pull_requests (read/write), issues (read/write)
GH_TOKEN=

# Git identity for commits made inside the container
GIT_USER_NAME=Claude Dev
GIT_USER_EMAIL=claude-dev@localhost
